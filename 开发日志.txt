今天犯了个低级错误，还发现了一个以前犯的错误，我把用来在shader中传输数据的仅存有void*类型的Var结构体升级成可以分配内存的Var类，但在测试代码时，我忽视了拷贝构造函数的重载，因此在内存释放上出了问题(一个地址被释放多次， 因为默认构造函数为浅拷贝， 而容器中插入数据时会调用拷贝构造函数)，我为此浪费了很多时间，好在最后发现了问题。我还发现了一个以前范的错误，我误以为glVertexAttribPointer最后一个参数和glDrawElement最后一个参数一样， 如果在没有绑定buffer对象时则为数组的地址，但事实上glVertexAttribPointer的最后一个参数只能是buffer的偏移量，所以我将原本正确的代码改成了错误的，现在我又要再改回去，还好，这工作量并不大。                           										——2023/9/27
震惊！在linux下给一个数组越界的地址赋值居然可以正常运行，若不是移植Windows平台时出现内存错误，我还一直无法发现这个可笑的错误。——2023/10/20
基本的功能已经实现了，但是现在有个严重的问题，那就是帧率太低(800x600不到1帧)，原因是我滥用map容器，map的插入与拷贝需要花费很多时间，而我几乎每个像素的遍历时都有这样的操作，这真是个严重的错误，我需要重新使用一种容器来表示着色其中的数据				——2023/10/24
我有些手足无措，不知道从哪儿改起，是否该重新开一个项目？这个项目的矩阵库设计的也有些问题，但我没有动力再重头来了。一开始我没有考虑效率问题，只想快点实现出效果，现在不知道如何去最大的优化，我可能会占时放下这个项目							——2023/10/25
